version: "3.8"  # Ensure you are using a valid Compose version

services:
  labelstudio:
    build: .  # Use the Dockerfile to build Label Studio
    container_name: label-studio
    ports:
      - "8080:8080"
    volumes:
      - ./data:/label-studio/data
      - ./data/media:/label-studio/data/media
      - ./data/export:/label-studio/data/export
      - ./data/uploads:/label-studio/data/uploads
      - ./config:/label-studio/config
    environment:
      - LABEL_STUDIO_LOCAL_FILES_SERVING_ENABLED=true
      - LABEL_STUDIO_LOCAL_FILES_DOCUMENT_ROOT=/label-studio/data
    restart: unless-stopped
